<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/appicon"
        android:supportsRtl="true"
        android:theme="@style/Theme.CSite"
        tools:targetApi="31">
        <activity
            android:name=".activities.MainScreen.SettingsActivity"
            android:exported="false" />
        <activity
            android:name=".activities.UserRegistrationAndLogin.PasswordActivity"
            android:exported="false" />
        <activity
            android:name=".activities.UserRegistrationAndLogin.UserRegistration"
            android:exported="false" />
        <activity
            android:name=".activities.MainScreen.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>